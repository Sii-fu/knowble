import 'package:flutter/material.dart';

class CourseApprovalPage extends StatelessWidget {
  const CourseApprovalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Course Approval')),
      body: const Center(child: Text('Course Approval Page')),
    );
  }
}
